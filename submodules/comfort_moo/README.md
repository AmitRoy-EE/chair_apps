# Prerequisites
You need a working installation of `conda` and `git`.

# Installation
0) `git clone https://gitlab.ruhr-uni-bochum.de/huckedyp/comfort_moo.git`
1) `conda env create -f env.yaml`
2) `conda activate comfort`
3) `streamlit run lit.py`

Play with the data 📈📊!